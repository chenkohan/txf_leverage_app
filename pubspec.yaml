name: txf_leverage_app
description: 台指期曝險槓桿計算器 - 計算台灣加權指數期貨的槓桿倍數

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_mobile_ads: ^5.3.0
  intl: ^0.19.0
  shared_preferences: ^2.3.0
  http: ^1.6.0
  in_app_purchase: ^3.2.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true